#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    FILE* fp;
    fp = fopen("test", "r");
    char buffer[1000];
    int index;
    char* temp;

    while(fgets(buffer, 1000, fp))
    {
        temp = strchr(string,'|');
        index = (int)(e - string);

        while(*
            ::
    }
}
